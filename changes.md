- Добавлен новый вариант кнопки `unsplash` в `Button.jsx` и `Button.module.css`, повторяющий стиль `LikeButton` (закругленные края, рамка, анимация).
- Добавлен вариант кнопки `rounded` для обычных действий (Выйти, Назад).
- В `Icon.jsx` добавлены иконки Unsplash и Logout.
- Кнопки входа, выхода и возврата назад обновлены для использования новых стилей и иконок.
- Настроен деплой на GitHub Pages: установлен пакет `gh-pages`, добавлены скрипты `deploy` и `predeploy`, настроен `basename` для `BrowserRouter`.
- Исправлена ошибка авторизации: `REDIRECT_URI` теперь определяется динамически, что позволяет авторизации работать как на `localhost`, так и на GitHub Pages.
- Исправлена ошибка 404 на GitHub Pages: добавлен скрипт перенаправления в `404.html` и `index.html`, что позволяет использовать `BrowserRouter` без ошибок при обновлении страницы или прямых ссылках.
- Исправлена ошибка `invalid_grant`: добавлен `useRef` в `AuthCallback` для предотвращения двойного запроса на обмен токена, а также уточнен расчет `REDIRECT_URI`.
- `REDIRECT_URI` возвращен к формату без фрагмента (`#`), так как Unsplash не поддерживает фрагменты в адресах обратного вызова.
